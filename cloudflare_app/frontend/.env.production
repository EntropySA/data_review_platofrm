# Production builds are served by the Worker that also serves /api/*, so the
# API base must stay empty. This file overrides any local .env during `build`,
# which would otherwise bake a localhost URL into the deployed bundle.
VITE_API_URL=
