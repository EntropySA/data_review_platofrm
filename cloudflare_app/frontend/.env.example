# Not needed for normal work. The dev server proxies /api to the local Worker
# and production serves both from one origin, so the API base stays empty in
# both. Set this only to point the app at some other deployed API.
VITE_API_URL=
